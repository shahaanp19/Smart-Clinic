@{
    ViewData["Title"] = "Admin Login";
}

<section class="login-section">
    <div class="container">
        <div class="row justify-content-center align-items-center min-vh-75">
            <div class="col-lg-5">
                <div class="smart-card login-card">
                    <div class="text-center mb-4">
                        <i class="bi bi-speedometer2 login-icon"></i>
                        <h1 class="dashboard-title mt-3">Admin Login</h1>
                        <p class="text-muted">Access clinic management and reporting.</p>
                    </div>

                    <form>
                        <div class="mb-3">
                            <label class="form-label smart-label">Email Address</label>
                            <input type="email" class="form-control smart-input" value="admin@smartclinic.co.za" />
                        </div>

                        <div class="mb-3">
                            <label class="form-label smart-label">Password</label>
                            <input type="password" class="form-control smart-input" value="password123" />
                        </div>

                        <div class="d-grid mt-4">
                            <a asp-controller="Admin" asp-action="Dashboard" class="smart-btn-primary text-decoration-none text-center">
                                Login as Administrator
                            </a>
                        </div>
                    </form>

                    <div class="text-center mt-4">
                        <p class="text-muted mb-0">Prototype only — dummy login interface</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>